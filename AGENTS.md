# Codex Instructions

Before any task, read `/docs/INDEX.md` and follow the documentation.

`/docs` is the single source of truth.

Do not implement anything that is not defined in `/docs`.

If unclear, ask or mark it as an assumption.
